{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal capabilities for the Eventra Mobile shell. The web frontend makes no Tauri IPC calls; window persistence, logging and external-link opening are handled entirely in Rust, so no plugin IPC is exposed to web content.",
  "windows": ["main"],
  "permissions": ["core:default", "opener:allow-open-url", "opener:allow-default-urls"]
}
