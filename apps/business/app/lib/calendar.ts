/**
 * Calendar grid/shift helpers now live in the shared @eventra/calendar engine
 * (single source). Re-exported here so existing `~/lib/calendar` imports work.
 */
export * from "@eventra/calendar";
