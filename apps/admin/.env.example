# Eventra Admin Console — environment (private web SPA). NO secrets client-side.
# Server-side admin auth/secrets live in the API/services env, never here.
VITE_EVENTRA_ENV=local            # local | test | development | staging | production
VITE_API_BASE_URL=                # admin API base (future); empty = mock/foundation
