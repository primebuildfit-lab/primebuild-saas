{
  "name": "@eventra/config",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Single source of truth for shareable platform config: products, plans, add-ons, entitlements, limits, trials. NO secrets.",
  "exports": {
    ".": "./src/index.ts"
  },
  "types": "./src/index.ts",
  "dependencies": {
    "@eventra/types": "*"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  }
}
