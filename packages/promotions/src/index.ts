/**
 * @eventra/promotions — shared promo template catalog + safe Liquid preview.
 * Pure data and string processing (no eval, no app/UI deps). Consumed by the
 * Admin console (performance stats) and the Business app (usable templates).
 */
export * from "./templates";
export * from "./render";
