/** Notification platform types (shared across surfaces). */

export type NotificationChannel = "in_app" | "push" | "email";

export type NotificationAudience = "consumer" | "business" | "admin";

export type NotificationImportance = "critical" | "optional";
