node_modules

# macOS
.DS_Store

# build outputs (any workspace)
/.cache
**/build
**/public/build/
**/public/_dev
**/dist

# Prisma dev DB (any location)
**/dev.sqlite
**/dev.sqlite-journal
database.sqlite

.env
.env.*
!.env.example

# Local file-mode persistence snapshots (MM4/MM5 dev only)
**/.eventra/


# Lockfile is committed. Keep npm's; ignore others.
yarn.lock
pnpm-lock.yaml

/extensions/*/dist

# Ignore shopify files created during app dev
**/.shopify/*
.shopify.lock

# Hide files auto-generated by react router (any app)
**/.react-router/

# Private local Claude Code permissions (never commit)
.claude/settings.local.json
