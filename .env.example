# Eventra environment — Phase 5 wiring (not required for Phases 1–4 mock UI).
# Copy to .env and fill in when connecting real Shopify + Supabase.

# --- Shopify app (Phase 5) ---
SHOPIFY_API_KEY=
SHOPIFY_API_SECRET=
SHOPIFY_APP_URL=
SCOPES=read_products,read_customers

# --- Supabase (separate Eventra project — NEVER primebuild-core) ---
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
DATABASE_URL=
